import { Area, AreaChart, CartesianGrid, ReferenceLine, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts';
import { formatDate, formatINR } from '../utils/formatters';

export default function PriceTrendChart({ trend = [], average, festiveAverage }) {
  if (!trend?.length) return <div className="glass rounded-2xl p-6 text-slate-300">No trend data available.</div>;
  const festiveOverlap = trend.some((item) => item.is_festive_period);
  return <article className="glass rounded-2xl p-6"><div className="mb-5"><h3 className="text-xl font-black text-white">60-Day Forward Price Trend</h3><p className="text-sm text-slate-400">India domestic fares from today, with festive markers</p></div><div className="h-80"><ResponsiveContainer width="100%" height="100%"><AreaChart data={trend}><defs><linearGradient id="priceFill" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stopColor="#3B82F6" stopOpacity={0.45}/><stop offset="100%" stopColor="#3B82F6" stopOpacity={0.02}/></linearGradient></defs><CartesianGrid stroke="rgba(255,255,255,0.08)" vertical={false}/><XAxis dataKey="date" tickFormatter={(value) => formatDate(value)} interval={9} stroke="#94A3B8"/><YAxis tickFormatter={(value) => '₹' + Math.round(value / 1000) + 'k'} stroke="#94A3B8"/><Tooltip content={({ active, payload, label }) => active && payload?.length ? <div className="rounded-2xl border border-white/10 bg-navy-800 p-3 shadow-xl"><p className="font-bold text-white">{formatDate(label, { year: 'numeric' })}</p><p className="text-brand-400">{formatINR(payload[0].value)}</p>{payload[0].payload.holiday_name && <p className="text-xs text-saffron">{payload[0].payload.holiday_name}</p>}</div> : null}/><ReferenceLine y={average} stroke="#F59E0B" strokeDasharray="6 6" label={{ value: 'Route Avg', fill: '#F59E0B', fontSize: 12 }}/>{festiveOverlap && festiveAverage && <ReferenceLine y={festiveAverage} stroke="#FF9933" strokeDasharray="6 6" label={{ value: 'Festive Avg Last Yr', fill: '#FF9933', fontSize: 12 }}/>}<Area type="monotone" dataKey="price" stroke="#60A5FA" strokeWidth={3} fill="url(#priceFill)" isAnimationActive /></AreaChart></ResponsiveContainer></div></article>;
}
